package org.example.footballmanager.demo.service.model;

/**
 * Action types for the simulation engine.
 * Corresponds to demo/Action.Type but independent.
 */
public enum ActionType {
    CHASE,
    CARRY,
    PASS,
    SHOT,
    CROSS,
    CENTER,
    AERIAL
}
