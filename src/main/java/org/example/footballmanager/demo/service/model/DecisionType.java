package org.example.footballmanager.demo.service.model;

/**
 * Decision types — what the carrier attempts to do.
 * Corresponds to demo/DecisionType but independent.
 */
public enum DecisionType {
    PASS,
    THRU,
    CARRY,
    CLEAR,
    SHOT,
    CROSS,
    CENTER
}
